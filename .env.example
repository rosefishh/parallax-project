# ──────────────────────────────────────────────────────────────
# Snare AI — environment reference
# Copy the relevant block into backend/.env and frontend/.env.
# NEVER commit real secrets. Both .env files are gitignored.
# ──────────────────────────────────────────────────────────────

# Backend (backend/.env)
# PORT=5000
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/parallax"
# DIRECT_URL="postgresql://postgres:postgres@localhost:5432/parallax"
# OCR_PROVIDER="tesseract"
# TESSERACT_CMD="C:/Program Files/Tesseract-OCR/tesseract.exe"
# FORENSICS_PYTHON="python"

# Frontend (frontend/.env)
# VITE_API_URL="http://localhost:5000/api"